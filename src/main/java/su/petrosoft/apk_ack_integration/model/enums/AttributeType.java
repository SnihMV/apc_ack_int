package su.petrosoft.apk_ack_integration.model.enums;

public enum AttributeType {
    BLOB, BOOLEAN, DATE, DICTIONARY, DOUBLE, LINKED, LONG, STATUS, STRING,
    RELATION, RELATION_DICTIONARY, DUPLICATE_ATTRIBUTE_ID
}
